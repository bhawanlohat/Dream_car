@page
@model Dream_car.Pages.Companys.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>

        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Company[0].Company_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Company[0].Company_Website)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Company[0].Company_Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Company[0].Company_Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Company[0].Company_Headquater)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model.Company)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Company_Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Company_Website)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Company_Phone)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Company_Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Company_Headquater)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
